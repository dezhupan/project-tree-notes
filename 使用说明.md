# Project Chinese Notes 1.0.6 使用说明

## 用途与环境

本插件在 PyCharm“项目”视图的文件和目录名称右侧显示中文说明，适合阅读陌生项目、教学项目和缩写较多的代码仓库。

- 宿主：PyCharm Professional 2026.1.x（内部版本 `261.*`）。
- 系统：Windows、macOS、Linux；本次安装包使用 Windows 上的 PyCharm 2026.1.1 SDK 构建。
- 普通说明功能完全离线；只有主动执行 AI 生成功能时才会访问你配置的 API。

## 安装与更新

1. 打开 PyCharm，进入“文件 → 设置 → 插件”。
2. 点击“已安装”页的齿轮按钮，选择“从磁盘安装插件”。
3. 直接选择 `project-tree-notes-1.0.6.zip`，不要解压该 ZIP。
4. 确认替换旧版本，并按 PyCharm 提示重启（如有提示）。

插件不会自动安装，也不会修改 PyCharm 的全局设置。

## 一键清除所有说明

1. 在“项目”视图中右键任意项目节点，打开“项目中文说明”子菜单；也可从“工具 → 项目中文说明”进入。
2. 选择“清除所有中文说明…”。
3. 确认窗口会显示将清除的说明条数；点击“全部清除”后一次性清空。

该操作只清空 `project_notes.tree.tsv` 第三列的中文说明，不会删除：

- 项目中的任何文件或目录；
- 说明表本身；
- 说明表里的类型和相对路径行。

如果当前没有非空说明，插件只会提示“没有可清除的中文说明”，不会写文件。确认窗口选择“取消”也不会产生修改。清除作为一次 PyCharm 写入命令执行，刚执行后可使用 IDE 的“撤销”恢复。

## 其他功能（保持不变）

- “编辑中文说明…”：编辑当前项目、目录或文件的一条说明；输入留空可删除该项说明。
- “AI 生成中文标签…”：选择范围、扫描安全内容、预览并确认后批量写入。
- “撤销上次 AI 生成”：只撤销最近一次 AI 批量写入，不覆盖之后的手工修改。
- “打开中文说明表”：打开并同步项目根目录唯一的 `project_notes.tree.tsv`。
- “AI API 设置…”：配置 API 地址、API Key、模型和扫描范围。

## 数据、隐私与恢复

- 所有说明保存在当前项目根目录的 `project_notes.tree.tsv`，插件不另建云端副本。
- API Key 使用 PyCharm 密码库保存，不写入项目或发布包。
- 卸载插件不会删除说明表；重新安装后可继续读取。
- 如需恢复误清除内容，优先立即使用 PyCharm“撤销”；也可通过 PyCharm 本地历史恢复 `project_notes.tree.tsv`。

## 卸载

进入“文件 → 设置 → 插件 → 已安装”，找到 Project Chinese Notes，选择禁用或卸载。插件不会替你删除项目中的 `project_notes.tree.tsv`。

## AI 单文件与目录生成、连接失败排查（1.0.6）

- 右键文件 → 项目中文说明 → AI 生成中文标签 → 当前文件，只处理该文件；选择目录则处理该目录及其可扫描子项。已有说明是否覆盖由 AI 设置决定。
- AI 结果仍先预览，点击确认后才写入说明表。
- 对 `Remote host terminated the handshake` 这种远端中断 HTTPS 握手的错误，自动等待 0.5 秒、1 秒各重试一次，最多三次尝试。单文件与目录使用相同策略。
- 证书校验失败、HTTP 401/429 等响应、普通超时及请求发送后的连接中断不会自动重试，避免重复请求。
- 若持续提示 HTTPS 握手失败，打开“AI API 设置…”测试连接，并核对 PyCharm 代理、API 地址和证书配置。不要关闭证书校验。
- 握手错误发生在 HTTPS 连接阶段，不能仅凭“单文件失败、目录成功”确定是文件内容问题。服务端或代理持续断开连接时，有限重试无法替代网络侧修复。
- 本次通过本地模拟故障和本机 HTTP 服务验证请求逻辑；未使用你的 API Key，也未对真实 API 或 PyCharm 界面完成端到端复测。
