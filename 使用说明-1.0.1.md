# Project Chinese Notes 使用说明

## 1. 这个插件解决什么问题

Project Chinese Notes 用于给 PyCharm 项目里的项目根目录、普通目录和文件添加简体中文说明。说明会直接显示在“项目”工具窗口的名称右侧，适合阅读机器学习项目、教学项目、陌生代码仓库和缩写较多的工程。

它和截图中的 Show Comment 工作方式不同：不用“创建外部注释 / 带树注释复制 / 添加行末注释”三套操作，只保留清楚的中文入口；也不会按你点击的先后顺序堆放记录。

## 2. 支持环境

- 宿主：PyCharm Professional 2026.1.x。
- 兼容内部版本：`261.*`。
- 系统：插件代码兼容 Windows、macOS 和 Linux；本次成品已在 Windows 上使用 PyCharm 2026.1.1 SDK 构建，并由官方 Plugin Verifier 对 PyCharm 2026.1.4 验证为 Compatible。
- 不支持 PyCharm 2025.x、2026.2 或其他内部版本；安装时 PyCharm 会阻止不兼容版本。

## 3. 分享包里有什么

- `project-tree-notes-1.0.1.zip`：PyCharm 识别的标准插件安装包。不要先解压这个内层 ZIP。
- `使用说明.md`：本说明文件。

源码工程中的 `build`、测试报告和 Gradle 缓存不是安装内容。

## 4. 安装步骤

1. 解压最外层分享包。
2. 打开 PyCharm。
3. 进入“文件 → 设置 → 插件”。
4. 点击“已安装”页右侧的齿轮按钮。
5. 选择“从磁盘安装插件”。
6. 选择内层的 `project-tree-notes-1.0.1.zip`，不要选择最外层分享包。
7. 按 PyCharm 提示确认；如果提示重启，再重启 PyCharm。

本成品没有替你安装，也没有修改 PyCharm 的现有设置。如果正在使用 Show Comment，建议先在“插件”中禁用它，避免两个插件同时给项目树追加说明。

## 5. 第一次使用

打开项目后，插件会在项目根目录创建唯一的：

```text
project_notes.tree.tsv
```

该文件会固定显示在项目树同级条目的底部。右键一个目录，例如 `02-rf`，选择“给目录添加中文说明…”，输入“随机森林训练与评估示例”，确定后会看到：

```text
02-rf  随机森林训练与评估示例
```

右键普通文件时，菜单文字会自动变成“给文件添加中文说明…”；右键项目根节点时会显示“给项目添加中文说明…”。输入框留空并确定，可删除该项原有说明。

## 6. 说明表格式与排序

打开 `project_notes.tree.tsv` 可以直接查看或修改第三列。格式如下：

```tsv
# Project Chinese Notes v1
# 本文件由插件维护顺序；可以直接修改第三列，保存后会刷新项目树。
类型	相对路径	中文说明
项目	.	机器学习课程示例项目
目录	01-data	训练和验证数据
文件	01-data/train.csv	模型训练样本
目录	02-rf	随机森林训练与评估示例
文件	02-rf/train.py	随机森林训练入口
```

关键规则：

- “项目 / 目录 / 文件 / 缺失”写在第一列，不再靠文件名猜类型。
- 相对路径统一使用 `/`，所以同名文件也能区分。
- 顺序来自项目树：项目根节点在前，目录优先，再按 `01`、`02`、`10` 这种自然顺序递归排列。
- 不论先注释 `10-llm` 还是先注释 `01-data`，同步后的顺序都相同。
- 路径被删除但仍有说明时，记录暂时标成“缺失”，避免插件静默删除笔记；重新创建同一路径后会恢复正确类型。
- 文件中出现制表符、换行或反斜杠时会分别保存为 `\t`、`\n`、`\\`。

建议只修改第三列，不手动改第一、二列。右键选择“打开项目中文说明表”会重新扫描项目并整理顺序。

## 7. 右键菜单

- “给项目/目录/文件添加中文说明…”：编辑当前选中项的一句说明。
- “AI 扫描此文件/此目录/整个项目并生成中文说明…”：扫描选中范围并生成候选说明。
- “打开项目中文说明表”：打开唯一 TSV，并触发一次结构同步。

插件会监听项目中的创建、删除、移动和重命名事件，自动补齐路径并重排说明表；只修改源码内容时不会做无意义的全树扫描。

## 8. 配置 AI

进入“文件 → 设置 → 工具 → 项目中文说明”。也可以使用主菜单“工具 → 项目中文说明：AI API 设置…”，或在项目树右键选择同名入口。配置：

- API 地址：可编辑下拉框，可选择 DeepSeek、OpenAI、阿里云百炼、Kimi、本机 Ollama，也可以输入其他兼容地址。插件会自动补 `/chat/completions`；Ollama 自动补 `/api/chat`。
- API Key：保存到 PyCharm 密码库，不会写入项目或 TSV。
- 模型名称：可编辑下拉框；选择 API 地址后自动切换为该服务的常用模型，也可以输入私有部署或新模型的真实 ID。自定义模型不会被强制覆盖。
- 测试连接：只发送一句固定测试文本，不扫描项目；结果直接显示在设置窗口中。
- 超时、每文件字符数、总字符数、单次条目数：大项目可按成本和模型上下文调整。
- “AI 默认重新生成已有说明”：默认关闭，避免覆盖手写笔记。

本机 Ollama 示例：

```text
API 地址：http://localhost:11434
API Key：留空
模型名称：你已经在 Ollama 中安装的模型名
```

其他本机 OpenAI 兼容服务也允许留空 Key。携带 API Key 的远程服务必须使用 HTTPS，HTTP 会被插件拒绝。

DeepSeek 当前下拉候选为 `deepseek-v4-flash` 和 `deepseek-v4-pro`；OpenAI 包含 GPT-5 系列和 `gpt-4.1-mini`。模型服务会持续更新，因此模型框始终允许手动输入。

## 9. AI 完整流程

1. 右键目标文件、目录或项目根节点。
2. 选择“AI 扫描…并生成中文说明…”。
3. 阅读联网范围提示，确认 API 地址和扫描范围。
4. 点击“开始扫描”。右下角会显示进度，可以取消。
5. 插件分批请求模型；返回后打开预览表。
6. 检查“原说明”和“AI 新说明”，可编辑新说明，也可取消某行的“写入”勾选。
7. 点击“写入已勾选说明”后，结果才会进入 TSV。

如果证据不足，提示词要求模型用“用途待确认”说明不确定性。模型返回的未知路径、空说明、非 JSON 内容不会写入。

## 10. 扫描、联网和隐私

普通手写说明与打开 TSV 都完全离线。只有你主动点击 AI 菜单、阅读提示并确认后，插件才访问配置的 API 地址。

AI 扫描会：

- 读取选中范围的相对路径、目录直接子项和文本文件前部片段。
- 排除 `.git`、`.idea`、虚拟环境、`node_modules`、缓存、常见构建输出和二进制文件。
- 不读取 `.env`、凭据、私钥、证书、密码库、数据库等敏感文件内容。
- 对普通源码中形似 API Key、Token、密码、Authorization 和带密码连接串的内容再次脱敏。
- 不跟随符号链接递归，防止扫描跑到项目目录之外。
- 不记录或输出完整 API Key。

被自动扫描排除的技术目录，如果它在项目树中可见，仍可通过右键手工添加中文说明。

## 11. 关闭、卸载、更新和恢复

- 临时关闭：进入“设置 → 插件 → 已安装”，找到 Project Chinese Notes 并禁用。
- 卸载：在同一位置选择卸载，按提示重启。
- 更新：从磁盘安装新版 ZIP，PyCharm 会提示替换旧版本。
- 卸载插件不会删除项目里的 `project_notes.tree.tsv`，手写笔记仍可恢复。
- 如果误写说明，可使用 PyCharm 的本地历史记录恢复 TSV，或直接在第三列改回。
- 如果不再需要说明表，请先卸载/禁用插件，再自行删除该项目中的 TSV；插件不会未经确认替你删除文件。

## 12. 常见问题

### 说明没有立刻显示

先保存 `project_notes.tree.tsv`，再右键选择“打开项目中文说明表”触发同步。仍未显示时，可折叠并重新展开父目录。

### 说明文件没有在最底部

确认使用的是 PyCharm 自带的“项目”视图，而不是第三方文件树视图；第三方视图不一定调用标准项目树扩展点。

### AI 提示没有 API Key

远程服务需要在设置页填写 Key。本机 `localhost` 或 `127.0.0.1` 服务可以留空。

### AI 返回 HTTP 错误

检查 API 地址、模型 ID、余额和服务商限流。插件只显示压缩后的错误摘要，不会把 Key 写进日志。

### 项目很大，AI 请求很多

先右键最需要理解的目录，而不是项目根节点；也可以在设置里调大“每次请求最多条目”，但不能超过模型上下文限制。

### 表里出现“缺失”

说明对应路径已删除或重命名，但原说明被保留。可以重新创建同一路径、把说明复制到新路径，或手工删除该行。
