# 测试与验收记录

## 构建环境

- Windows
- 本机 PyCharm 2026.1.1，内部版本 `PY-261.23567.174`
- JetBrains Runtime 25.0.2
- Gradle 9.0.0
- Java 21 字节码目标

## 自动化测试

执行命令：

```powershell
.\gradlew.bat clean coreTest verifyPlugin buildPlugin -PlocalIdePath='<PyCharm安装目录>'
```

核心测试结果：18/18 通过，0 跳过，0 失败。覆盖：

- 项目、目录、文件、缺失四种 TSV 条目往返解析。
- 制表符、换行和反斜杠转义。
- 注释添加顺序与最终项目树顺序无关。
- 数字目录名的自然排序。
- 已删除路径的有内容说明保留，空说明清理。
- `.env`、私钥、凭据、数据库和二进制文件内容拦截。
- 普通源码中的 API Key、Token、密码和连接串脱敏。
- AI 请求按条目数切批并携带真实源码证据。
- Markdown 代码围栏内 JSON 的兼容解析。
- AI 返回未知路径时拒绝写入该路径。
- AI 返回非 JSON 时整体拒绝，不把猜测写入 TSV。
- 中文说明只作为右侧位置文字，原目录名、文件名和项目根路径保持不变。
- API 地址预设互不重复且都带可选模型。
- 选择 DeepSeek 地址时联动显示 V4 Flash/Pro，手动地址和私有模型保持可编辑。
- OpenAI 兼容地址和 Ollama 自动拼接不同的请求端点。
- 远程 API Key、HTTPS、本机免 Key 和非法地址校验。

## 插件结构与兼容性

- `compileJava`：通过，无弃用 API 警告。
- `verifyPlugin`：通过。
- 官方 Plugin Verifier 1.409：`Compatible`。
- 校验目标：PyCharm `PY-261.26222.68`（2026.1.4）。
- 动态插件结论：可以在不重启 IDE 的情况下启用或禁用（PyCharm 仍可能自行提示重启）。
- 标准安装包：`build/distributions/project-tree-notes-1.0.1.zip`。

## 用户要求对应关系

| 用户要求 | 实现与验收 |
| --- | --- |
| 注释文件在最底部且只有一个 | 固定使用项目根目录 `project_notes.tree.tsv`；项目树结构扩展把该节点移到同级末尾 |
| 所有目录和文件可注释 | 项目、普通目录和文件均有动态中文右键入口；技术目录即使不参加 AI 自动扫描，仍可手写说明 |
| 目录和文件容易区分 | TSV 第一列明确写“项目 / 目录 / 文件 / 缺失” |
| 按项目顺序而非注释顺序排列 | 每次同步从项目树重建路径顺序，再按路径匹配保留说明 |
| 不使用旧插件三套晦涩动作 | 改为“给项目/目录/文件添加中文说明”“AI 扫描…”和“打开项目中文说明表” |
| 大模型理解真实项目而非只看名称 | 扫描目录直接子项和安全源码片段，分批请求，并要求证据不足时标注待确认 |
| AI 可从右键进入 | 文件、目录和项目根节点均提供动态 AI 菜单 |
| AI 不应直接覆盖 | 联网前确认范围，结果在可编辑勾选表中预览，确认后才写入 |
| API 地址和模型应可选择 | 设置页提供联动、可编辑的双下拉框，并可从设置、工具菜单和右键打开 |
| 应能验证配置 | “测试连接”只发送固定测试文本，在设置窗口内显示中文结果 |

## 未执行的操作

- 没有把插件安装到用户正在使用的 PyCharm。
- 没有修改用户的全局 PATH、注册表或 PyCharm 设置。
- 没有调用真实 AI 服务，因此没有消耗用户 Token，也没有发送任何项目源码。
